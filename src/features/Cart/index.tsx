import React from 'react';
import './index.scss';
import { Container } from 'reactstrap';

interface Props {}

export const Book = (props: Props) => {
  return (
    <Container>
    </Container>
  );
};
