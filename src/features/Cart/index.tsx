import React from 'react';
import './index.scss';
import { Container } from 'reactstrap';

interface Props {}

export const Cart = (props: Props) => {
  return (
    <Container>
    </Container>
  );
};
