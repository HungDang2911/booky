import React from 'react';
import './Footer.scss';
import { Container } from 'reactstrap';

interface Props {}

export const Footer = (props: Props) => {
  return (
    <Container>
    </Container>
  );
};
